Why: Created as a sidecar Expo/React Native client shell adapted from `happy-app` so GhoDex can borrow a mature mobile UX without replacing the existing desktop gateway and auth model.
Who: Codex GPT-5
Creation Date: 2026-03-23
Expected Scope: Mobile client fork that will replace Happy-specific auth/sync flows with `ControlHarnessGateway` pairing, snapshot, subscribe, and mutation adapters.
